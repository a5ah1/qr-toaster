# Nginx configuration for local testing of QR Toaster
#
# Installation:
# 1. Copy this file: sudo cp nginx.conf.example /etc/nginx/sites-available/qr-toaster
# 2. Enable site: sudo ln -s /etc/nginx/sites-available/qr-toaster /etc/nginx/sites-enabled/
# 3. Test config: sudo nginx -t
# 4. Reload Nginx: sudo systemctl reload nginx
# 5. Add to /etc/hosts: 127.0.0.1 qr-toaster.local
# 6. Access at: http://qr-toaster.local

server {
    listen 80;
    listen [::]:80;

    # Change to your custom domain for testing, or use .local for development
    server_name qr-toaster.local;

    # Update this path to your actual project location
    root /home/danny/dev/qr-toaster/_site;

    index index.html;

    # Logging (optional, useful for debugging)
    access_log /var/log/nginx/qr-toaster-access.log;
    error_log /var/log/nginx/qr-toaster-error.log;

    # Main location block
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets aggressively (CSS, JS, images, fonts)
    location ~* \.(css|js|svg|png|jpg|jpeg|gif|ico|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # Content Security Policy (adjust as needed for your QR libraries)
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:;" always;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json image/svg+xml;
}

# Optional: HTTPS configuration (requires SSL certificate)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#
#     server_name qr-toaster.local;
#
#     ssl_certificate /path/to/cert.pem;
#     ssl_certificate_key /path/to/key.pem;
#
#     # Include all location blocks from above
#     root /home/danny/dev/qr-toaster/_site;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     # ... (copy other location blocks)
# }
